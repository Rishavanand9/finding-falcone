import loading from './loading.gif'
import './Loader.css'

const Loader = () => {
  return (
    <div className="loader">
      <img src={loading} alt="loader" />
    </div>
  )
}

export default Loader
